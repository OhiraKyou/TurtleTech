package ohirakyou.turtletech.data;

public final class DataGuiIDs {
    // These are initialized explicitly because switch statements require them to be compile time constants
    public static final int SOUL_TURBINE_GENERATOR = 0;
}
